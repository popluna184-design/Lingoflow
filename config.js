/* ── LingoFlow Shared Config ──
   Set USE_GLOBAL_KEY = true and fill in your GLOBAL_KEY
   to let friends use the app without entering their own key. */

const USE_GLOBAL_KEY = true;
const GLOBAL_KEY = 'sk-你的DeepSeek密钥填在这里';
